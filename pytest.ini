[pytest]
minversion = 7.0
addopts = -q
markers =
    unit: Unit tests (fast, no external services)
    smoke: Lightweight live checks (may hit running gateway)
    integration: Cross-service tests (require running stack)
    performance: Performance benchmarks (opt-in)
    security: Security checks and policies
